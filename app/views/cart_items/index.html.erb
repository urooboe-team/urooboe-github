<h1>ショッピングカート</h1>
<%= link_to"カートを空にする", cart_item_path(@cart_items), method: :delete  %>
<table>
	<tr>
	  <th>商品名</th>
	  <th>単価（税込）</th>
	  <th>数量</th>
	  <th>小計</th>
	  <th></th>
	</tr>
	<% @cart_items.each do |item| %>
	<tr>
	  <td>
	  	<%= item.product.product_name %>
	  </td>
	  <td>
	  	<%= item.product.unit_price %>
	  </td>
	  <td>
	  	<%= form_with model: item, local: true do |item_quantity| %>
			<%= item_quantity.text_field :quantity %>
			<%= item_quantity.submit '変更する' %>
		<% end %>
	  </td>
	  <td>
	  	<%= item.product.unit_price * item.quantity %>
	  </td>
	  <td>
		<%= link_to "削除する", cart_item_path(item), method: :delete %>
	 </td>
	 </tr>
	 <% end %>
</table>
